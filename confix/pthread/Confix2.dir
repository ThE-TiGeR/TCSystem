# -*- python -*-

PROVIDE_SYMBOL('TCSYSTEM_PTHREAD')

CONFIGURE_AC(
    lines=['ACX_PTHREAD'],
    order=AC_LIBRARIES)

EXTERNAL_LIBRARY(
    cflags=['$(PTHREAD_CFLAGS)'],
    cxxflags=['$(PTHREAD_CFLAGS)'],
    libs=['$(PTHREAD_LIBS)'])
